// Generated by @colibri/contract-bindings. Replaceable: keep custom code outside this file.
/**
 * Typed Colibri client for Counter.
 * @module
 */
import {
  Contract,
  type ContractId,
  createContractErrorMatcherPlugin,
} from "@colibri/core";
import { Spec } from "@colibri/core";
import { ContractMethods, CounterErrors, CounterSpec } from "./constants.ts";
import type {
  CounterCall,
  CounterConstructorArgs,
  CounterEvents,
  CounterInvocation,
  CounterInvocationResult,
  CounterMethod,
  CounterMethodMap,
  CounterOutputs,
} from "./types.ts";

export * from "./constants.ts";
export * from "./types.ts";
export {
  ColibriError,
  LocalSigner,
  NetworkConfig,
  SorobanType,
} from "./colibri.ts";
export type {
  AuthEntrySigner,
  ContractConfig,
  ContractConstructorArgs,
  ContractId,
  Ed25519PublicKey,
  EnvelopeSigner,
  KeypairSigner,
  Signer,
  TransactionConfig,
} from "./colibri.ts";

/** Simulate or invoke callable functions declared in the embedded contract spec. */
export class Counter extends Contract {
  /** Read a structured summary with a named status enum. */
  readonly summary: CounterMethod<"summary"> = {
    read: (methodArgs) =>
      this.read({ method: ContractMethods.Summary, methodArgs }),
    invoke: (args) =>
      this.invoke({
        ...args,
        method: ContractMethods.Summary,
      }),
  };

  /** Read the current count, initially zero. */
  readonly getCount: CounterMethod<"get_count"> = {
    read: (methodArgs) =>
      this.read({ method: ContractMethods.GetCount, methodArgs }),
    invoke: (args) =>
      this.invoke({
        ...args,
        method: ContractMethods.GetCount,
      }),
  };

  /** Increase the count by a positive amount, up to a maximum of 100. */
  readonly increment: CounterMethod<"increment"> = {
    read: (methodArgs) =>
      this.read({ method: ContractMethods.Increment, methodArgs }),
    invoke: (args) =>
      this.invoke({
        ...args,
        method: ContractMethods.Increment,
      }),
  };

  /** Return a summary unchanged to exercise named input and output types. */
  readonly echoSummary: CounterMethod<"echo_summary"> = {
    read: (methodArgs) =>
      this.read({ method: ContractMethods.EchoSummary, methodArgs }),
    invoke: (args) =>
      this.invoke({
        ...args,
        method: ContractMethods.EchoSummary,
      }),
  };

  /** Install a fresh spec and the prepared error map alongside existing plugins. */
  constructor({ errors = CounterErrors, ...args }: CounterConstructorArgs) {
    const contractId = args.contractConfig.contractId as ContractId | undefined;
    const matcher = errors === false || Object.keys(errors).length === 0
      ? undefined
      : createContractErrorMatcherPlugin(
        contractId ? [{ strategy: "contract-id", contractId, errors }] : [{
          strategy: "issued-from",
          issuedFrom: "root-invocation",
          errors,
        }],
      );
    const plugins = args.contractConfig.plugins;

    super({
      ...args,
      contractConfig: {
        ...args.contractConfig,
        spec: new Spec(
          CounterSpec.entries.map((entry) => entry.toXdr("base64")),
        ),
        plugins: {
          invokePipe: [
            ...(matcher ? [matcher] : []),
            ...(plugins?.invokePipe ?? []),
          ],
          readPipe: [
            ...(matcher ? [matcher] : []),
            ...(plugins?.readPipe ?? []),
          ],
        },
      },
    });
  }

  /** Access event definitions bound to the current contract ID. */
  override get events(): CounterEvents {
    return super.events as CounterEvents;
  }

  /** Simulate a function and decode its result without submitting a transaction. */
  override async read<Method extends keyof CounterMethodMap>(
    args: CounterCall<Method>,
  ): Promise<CounterOutputs[Method]> {
    return await super.read(args) as CounterOutputs[Method];
  }

  /** Submit through Colibri and retain both the decoded value and raw result. */
  override async invoke<Method extends keyof CounterMethodMap>(
    args: CounterCall<Method> & CounterInvocation,
  ): Promise<CounterInvocationResult<CounterOutputs[Method]>> {
    const result = await super.invoke(args);
    return this.decodeInvocationResult<CounterOutputs[Method]>(
      args.method,
      result,
    );
  }
}
